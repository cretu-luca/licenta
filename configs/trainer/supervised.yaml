_target_: src.training.trainer.SupervisedTrainer
name: supervised
lr: 0.0005
batch_size: 1024
num_epochs: 400
patience: 30
weight_decay: 1.0e-4
device: auto
log_every_n_epochs: 1
grad_clip_norm: 1.0
num_workers: 0
scheduler:
  name: plateau
  factor: 0.5
  patience: 5
  min_lr: 1.0e-6
